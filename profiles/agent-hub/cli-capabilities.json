{
  "schemaVersion": 1,
  "contract": "kfd.agent-hub-cli-capabilities/v1",
  "profile": "kfd-agent-hub-conformance@0.1.0-alpha.1",
  "binding": "jsonl-stdio/v1",
  "commands": {
    "demo": "kfd demo agent-hub --output <report.json> [--reference-adapter state-machine|rule-table] [--json]",
    "scaffold": "kfd scaffold agent-hub --language <cpp|node|python|rust> --output <new-directory> [--json]",
    "test": "kfd test agent-hub --adapter <path> --output <report.json>",
    "verify": "kfd verify agent-hub-report <report.json> [--adapter <path>] [--json]",
    "kungfuProduct": "kungfu agent hub qualify --output-dir <new-directory> [--json]"
  },
  "scaffoldLanguages": ["cpp", "node", "python", "rust"],
  "exitCodes": {
    "0": "suite passed, report verified, or scaffold completed",
    "1": "execution completed but a conformance or verification check failed",
    "2": "invalid invocation, unsafe path, malformed JSON, process failure, or envelope failure"
  },
  "reportVerification": {
    "backend": "host-node",
    "processSpawning": "host-only",
    "sharedRustWasmChecks": [],
    "parityBoundary": "No Agent Hub report check is duplicated in the Rust/WASM verifier. The single host verifier binds installed package bytes and optional adapter bytes; native/WASM verification-bundle profiles remain separate semantic authorities. Moving this profile requires a versioned bundle kind and byte-for-byte native/WASM parity before the host verifier can be retired."
  },
  "claimBoundary": "Demo success and generated starter smoke success are non-qualifying, non-certifying evidence. Only kfd test agent-hub executes Hub 20 against the named adapter artifact. Kungfu may project that report into an exact installed-product qualification, but it does not turn the result into KFD certification, a security assessment, production fitness, remote-network interoperability, or external adoption.",
  "firstPartyProductProjection": {
    "product": "Kungfu",
    "run": "kungfu agent hub qualify --output-dir <new-directory>",
    "verify": "kungfu agent hub verify --qualification-dir <directory>",
    "ownership": "KFD owns the fixed suite and offline report verifier; Kungfu owns its product semantics, isolated execution, installed-artifact binding, and human or agent explanation."
  },
  "recovery": "Delete only the newly generated scaffold directory or report you created; kfd never overwrites an existing scaffold path."
}
