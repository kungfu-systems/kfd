{
  "schemaVersion": 1,
  "contract": "kungfu-buildchain-impact",
  "release": {
    "line": "v1.0",
    "channel": "release",
    "targetRef": "release/v1/v1.0"
  },
  "versionImpact": {
    "final": "patch",
    "source": "kfd-surface-register",
    "rationale": "The v1.0 production release preserves the current KFD content and registry schema surfaces."
  },
  "surfaceImpacts": [
    {
      "id": "kfd-content",
      "impact": "patch",
      "class": "content",
      "rationale": "KFD documents are append-only content coordinates; current decision text changes do not alter a machine-consumed surface."
    },
    {
      "id": "kfd-registry-schema",
      "impact": "patch",
      "class": "compatible",
      "rationale": "registry.json keeps schemaVersion 1 and the existing entry shape, including number, id, slug, title, kind, status, path, and url."
    },
    {
      "id": "kfd-package-structure",
      "impact": "patch",
      "class": "compatible",
      "rationale": "The npm package continues to publish decisions/, registry.json, and docs/ without changing the package surface."
    }
  ],
  "summary": "KFD v1.0 production promotion is patch-impact across the registered KFD content, registry schema, and package structure surfaces."
}
